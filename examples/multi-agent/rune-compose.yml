version: "1"
project: multi-agent

rune-env:
  LOG_LEVEL: info
  AGENT_PACKAGES_DIR: "."

agents:
  router:
    source: ./router
    alias: stable
    replicas: 1

  researcher:
    source: ./researcher
    alias: stable
    replicas: 1

  analyst:
    source: ./analyst
    alias: stable
    replicas: 1
